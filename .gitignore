# Credential / session leaks — NEVER commit these
*.har
*.sqlite
*.sqlite-*
cookies*
*token*
*.pem
*.key
.env
.env.*
auth.json
*-auth-accounts.json
oauth_creds.json

# Multi-account: real tokens + rate-limit state (never commit)
qwen-accounts.json
qwen-accounts-state.json

# Diagnostic logs
qwen-chat-debug
qwen-chat-debug.log

# OpenCode local state
opencode.json
opencode.jsonc
opencode-go.json

# Node
node_modules/
npm-debug.log*
bun.lockb

# OS / editor
.DS_Store
*.swp
.idea/
.vscode/
